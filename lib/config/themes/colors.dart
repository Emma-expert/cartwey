import 'package:flutter/material.dart';

class MyColors{
  //this the Primary green Color
  static const Color primaryColor = Color.fromRGBO(13, 76, 59, 1);

  //this is the Secondary color, the yellow color
  static const Color secondaryColor = Color.fromRGBO(249, 189, 0, 1);

  //this is the Onsuccess color
  static const Color onsuccessColor = Color.fromRGBO(164, 244, 231, 1);

  //this is the warning color
  static const Color warningColor = Color.fromRGBO(244, 199, 144, 1);

  //this is the error color
  static const Color errorColor = Color.fromRGBO(228, 98, 111, 1);

  //this is the text color
  static const Color customtextColor =   Color.fromRGBO(26, 28, 30, 1);

}